// Configuration constants - centralized magic values
export const CONFIG = {
    // Number of regions
    REGION_COUNT: 9,

    // Highest tier available
    MAX_TIER: 10,

    FLOWCHART_ZOOM: {
        MIN: 0.25,
        MAX: 2,
        STEP: 0.1,
        WHEEL_SENSITIVITY: 0.0005
    }
};